<form action="/faelle" method="post">
  <label for="cards">Wähle ein Urteil:</label>
  <select id="cards" name="cardId">
    {{#each cards}}
    <option value="{{_id}}">{{Urteil}}</option>
    {{/each}}
  </select>
  <button type="submit">Suchen</button>
</form>

{{!-- todo add single page logic --}}